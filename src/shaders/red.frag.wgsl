@fragment
fn main() -> @location(0) vec4<f32> {
    return vec4<f32>(
        1.0, //Red   (R)
        1.0, //Green (B)
        1.0, //Blue  (G)
        1.0  //Alpha (A)
    ); // returns a solid red color 
}
